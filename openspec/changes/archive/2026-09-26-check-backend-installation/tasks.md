## 1. Contract and executable resolution

- [x] 1.1 Add domain installation input/result/issue/report types, typed PreflightError and mandatory AgentAdapter.CheckInstallation; update every adapter and test double, verifying compile-time conformance and explicit fake/external not_required behavior.
- [x] 1.2 Share effective launch-plan construction between checks and spawning, preserving machine precedence, workspace and nil-versus-explicit environment rules for every CLI, separately preserving managed OpenCode baseline inheritance; verify with machine-default and adapter tests where server PATH differs from child PATH and agent options override machine settings; cover default ambient PATH and absent inherit_env entries producing nil.
- [x] 1.3 Implement name/absolute/relative executable resolution, literal spaces, no expansion/fallback, PATH empty/relative-entry rules including mixed absolute/relative rejection, symlink and effective-user access checks; verify temporary-file tests for missing files, directories, broken/looping links, permissions and explicit bad paths despite a valid default on PATH.
- [x] 1.4 Check recognized shebang interpreters without running wrapper contents, with bounded recursion/cycle detection and unsupported-launcher reporting; verify direct interpreter, env NAME, simple env -S and missing interpreter fixtures, plus native Windows unsupported-platform behavior and Linux runtime_path guidance.

## 2. Backend checks and bounded aggregate preflight

- [x] 2.1 Implement Codex/Cursor/Kimi executable checks and fake no-op with fixed official links; verify self-contained binaries do not require Node/Python and Cursor command overrides do not silently fall back to another alias.
- [x] 2.2 Add ZCode probe-only inspection before runtime compilation/account access, including independent Node/runtime/provider-config failures and structural compatibility; extend host_test.go fixtures to prove unsupported/ambiguous bundles are not loaded and valid probes never read account data or spawn app-server, and environment-driven Node preload hooks do not execute during inspection.
- [x] 2.3 Implement per-pass deduplication, deterministic aggregation and affected-agent attribution; verify identical installations across model/prompt/name differences deduplicate, while distinct paths, modes, environment and workspace remain independent and no key values reach logs/artifacts.
- [x] 2.4 Enforce five-second local checks, separate thirty-second installation and sixty-second readiness phase budgets including cleanup, four-check concurrency, bounded output and cancellation; verify injectable slow/stuck probes and simultaneous missing components retain completed issues, mark unfinished checks and cannot release work; verify cold startup/config receives its full phase budget and earlier cancellation/deadline carries correct latch diagnostics.
- [x] 2.5 Integrate with the separately delivered OpenCode effective mode/launch plan and owned-service readiness hook without implementing its defaults/supervisor here; verify managed missing CLI fails, external absent CLI passes locally, no owned server starts on any local failure, failed/cancelled first startup remains latched until explicit Squad restart, and successfully started shared servers survive a later admission failure.
- [x] 2.6 Add bounded health-protocol readiness with phase-specific errors using the existing/other change's transport; verify httptest cases for healthy JSON with or without optional version (no tightening of the managed health hook), refused connection, non-2xx, redirects, malformed health, timeout and no session/prompt calls.

## 3. Manual lifecycle and API errors

- [x] 3.1 Remove eager external initialization from orchestrator.New and defer replacement session creation after reset; verify unused OpenCode/ZCode do not block serving and manual session continuity, startup instructions and force-reset cleanup remain correct.
- [x] 3.2 Gate SubmitRun with a cancellable per-agent admission reservation before run persistence or facilitator transcript writes; verify missing installation, concurrent submits, reset/shutdown races and cancellation produce no unauthorized worker or leaked reservation.
- [x] 3.3 Gate SubmitOwnedRun before external Init, accepting only an internal single-dispatch prepared plan to avoid redundant immediate checks; verify direct callers cannot bypass preflight, owned runs retain fresh sessions and saved specs still receive current startup machine defaults.
- [x] 3.4 Map typed reports to structured HTTP 503, cancelled/disconnected request handling without synthetic 408/499 responses, and retain existing validation/conflict/storage codes and CLI startup exit 2 and restart_required diagnostics; verify API response fields, deterministic text and sentinel redaction across HTTP, logs, probe output and persisted diagnostics.

## 4. Workflow admission and recovery

- [x] 4.1 Add context-aware Executor preflight and Manager/API Create/Resume/Retry signatures, selecting all referenced agents for initial admission and potentially runnable agents for recovery/resume, and only the target for retry acceptance; verify downstream/optional/loop agents are included while unused agents and permanently completed work are excluded.
- [x] 4.2 Gate Create after structural/idempotency/conflict checks and before snapshot/request-ID commitment; verify 202/200/409 behavior, concurrent duplicate waiting, failed admission followed by repair with the same request ID, and zero first-task calls when any referenced installation fails.
- [x] 4.3 Add recovery/resume/retry installation holds and optional sanitized backend_preflight snapshot/view diagnostics without changing definition hashes or schema versions; verify old snapshots, preserved pause/other holds, retry replay, no consumed rejected retry ID, repair/resume and listener availability during slow asynchronous recovery checks and after failure; reject multiple nonterminal saved executions before checking and ignore terminal history.
- [x] 4.4 Check ready candidates outside the scheduler lock before each batch reservation and revalidate revision/gates afterward; verify late executable removal creates no new attempts, concurrent running work can finish and pause/cancel/stale results never release work.
- [x] 4.5 Preserve post-check launch-failure and uncertain cleanup semantics without fallback or automatic prompt retry; verify disappearance between final check and spawn, manual continuity, ephemeral fresh attempts and conservative recovery tests; verify an eligible healthy target retry queues despite unrelated installation holds and does not clear their diagnostics.

- [x] 4.6 Integrate preflight with main's existing internal/oneshot startup/selection/recovery flow, preserving terminal replay, summary schema and signal precedence; verify subprocess tests for pre-selection exit 2 with aggregate stderr/null execution summary/no summary file, recovered needs_attention remaining alive and cleanup-error exit 1.
- [x] 4.7 Expose checking/failed backend_preflight view diagnostics and restart_required intervention guidance under the observation contract; verify long-poll wakeup, ordinary repair/resume and latched-runtime restart guidance without raw values.

## 5. Documentation and verification

- [x] 5.1 Update README and relevant examples with concise official installation links, effective child PATH rules, Cursor explicit command guidance, manual/workflow 503 examples, recovery holds, one-shot run reporting, startup-latch restart guidance and OS boundaries; verify examples remain structurally valid without installed CLIs and no installer/model requests occur; explain stricter public-preflight path redaction versus existing invocation provenance.
- [x] 5.2 Run focused adapter/orchestrator/workflow/API integration tests using temporary executables, fixture bundles and local HTTP servers; measure repeated batch overhead without a cross-pass cache and verify every new requirement/scenario has evidence and no paid API or real account is needed (run ZCode structural fixture tests with Node available in CI).
- [x] 5.3 Run gofmt on every changed Go file, then `go vet ./...` and `go test -race -count=1 ./...`; verify successful completion and resolve failures before implementation handoff.
- [x] 5.4 Run `openspec validate check-backend-installation --strict`, compare the final implementation against both delta specs, and reconcile artifacts with actual behavior and re-read main plus merged managed OpenCode specs before sync, preserving all one-shot requirements and complete modified baseline blocks; verify all required checks pass before using openspec-archive-change to sync and archive the completed change.
