# Tasks: add-workflow-loop-conditions

## 1. Domain and configuration

- [x] 1.1 Add `UntilTask`, `OnVerdict` (map verdict→action), and `OnExhaustion` to `WorkflowLoopDefinition` (JSON `omitempty`); add `ExtendedIterations` and `StopRequested` (`omitempty`) to `WorkflowLoopExecution` and an effective-cap helper; extend `WorkflowLoopView` with `until_task`, `effective_max_iterations`, and `last_condition_verdict`, plus `stop_requested`. Verify snapshot round-trip and view tests retain the new fields.
- [x] 1.2 Parse the condition fields in `rawWorkflowLoop` under the strict workflow subtree. Verify config tests: fields parse, unknown loop fields still fail, definitions without conditions hash byte-identically (golden), and each condition field change alters the definition hash.
- [x] 1.3 Extend `ValidateWorkflowDefinition` with the condition rules via `loopErrorf` examples: `until_task` must be a body member declaring `verdicts`, the unique body sink reached from every other body task through same-loop needs edges (singleton allowed), and mandatory (reject `allowed_to_fail: true`, accept omitted/false while other body tasks may remain optional); `on_verdict` must exactly cover the declared verdict set (missing and unknown keys named); actions limited to `break`/`continue`/`needs_attention`; `until_task`/`on_verdict` must pair and explicit `on_exhaustion` requires that pair; `on_exhaustion` limited to `needs_attention` (default) or `succeed`, rejecting `fail`. Verify table-driven tests for every rejection (including error name-and-example content) and acceptance of valid conditions.

- [x] 1.4 Rename the new-YAML uncertainty policy and effective default from hold to needs_attention, retaining error. Update domain constants, config validation/error examples, judge logic, README, maintained skill instructions, examples, and tests. Reject explicit hold in saved definitions before scheduling as well as in YAML; add no alias, fallback, or automatic migration. Verify new spelling/default behavior, old YAML rejection, rejection of saved definitions with hold without dispatch or mutation, unchanged omitted-policy hash, and changed-definition conflict when an explicit policy spelling changes under the same submission request ID.

## 2. Condition evaluation engine

- [x] 2.1 In `advanceLoopsLocked`, read the condition verdict of `until_task`'s committed current-iteration attempt on acceptable settlement and apply the mapped action: `break` marks done at the current iteration; `continue` advances below the effective cap and otherwise triggers the exhaustion policy; `needs_attention` holds without advancing. Verify manager tests including needs_attention at the cap with on_exhaustion succeed still holding; break at iteration 1 of 3 (outside consumers get iteration-1 results), repeat-until-clean over three iterations, and static loops byte-identical behavior.
- [x] 2.2 Implement exhaustion policies: `succeed` completes like break; default `needs_attention` derives `loop_exhausted:<loop>` with extend/stop/cancel guidance. Verify both policies, default selection, recovery re-derivation of the hold, and that succeed preserves the condition verdict and ordinary tolerated-failure accounting rather than forcing workflow success. Do not add an exhaustion-failure outcome field.
- [x] 2.3 Keep all declared verdict names ordinary: blocked is allowed and requires the same complete mapping on until_task as any other verdict. Verify blocked/needs_input do not stop non-condition tasks, an explicit needs_input → needs_attention condition mapping does hold, absent verdicts cause no classification or structured help-request hold, and classification still runs with declared options lacking an information-request option. Update the existing no-verdict-scheduling requirement/tests to exempt explicit condition mappings only.
- [x] 2.4 Confirm precedence and non-evaluation: `loop_failure`/`loop_blocked` holds, judging attempts, uncertain verdicts, and judge unavailability all prevent condition evaluation. Verify manager tests with a held judge on the condition task, a condition timeout/backend failure/missing response, and low-confidence classification with `on_uncertain: error`. Failed condition attempts must hold for retry without looking up a missing or synthetic uncertain verdict or applying exhaustion policy.

## 3. Verdict override extension

- [x] 3.1 Extend `OverrideVerdict` to accept settled attempts whose verdict is load-bearing on a live hold (a verdict holding a condition via `needs_attention`): rewrite the verdict with manual source, keep the attempt settled, re-derive holds and conditions on the next reconcile. Attempts from iterations the loop has advanced past stay rejected. Verify manager tests: ordinary settled blocked/needs_input verdicts on non-condition tasks remain non-overridable; override redirects a held condition to continue below cap, applies each exhaustion policy at cap without an extra iteration, and preserves unrelated sibling holds; history immutability after advance; idempotent replay and 409 conflicts.

## 4. Loop controls

- [x] 4.1 Implement `POST /workflows/{id}/loops/{name}/extend` (manager method + route): positive `add_iterations`, request-ID idempotency via control events, 400/404/409 mapping, durable `ExtendedIterations` increase persisted before effects, `loop_exhausted` release on the next reconcile. Commit the raised cap and replay record atomically; look up accepted requests before lifecycle rejection and compare both target loop and amount. Verify manager/API tests for exhaustion release, duplicate replay, replay after completion/cancellation and restart, conflicting loop or amount (409), new requests on done/terminal/cancelling states (409), invalid input (400), unknown loop (404), and save-failure recovery with no double increase or dispatch on an uncommitted cap.
- [x] 4.2 Implement stop as durable intent to finish the current iteration without starting another. Continue normal body scheduling, preserve pause and failure/interruption/judge holds, and release outside consumers only after acceptable settlement. Resolve an eligible stop locally before the global dispatch gate even while sibling loops are held; remove only its own condition/exhaustion cause and preserve all global dispatch restrictions. Check accepted-request replay before lifecycle rejection; reject new requests on done/terminal/cancelling state and conflicts across loop targets. Verify active and pending body work, failure repair, condition/exhaustion holds, pending classification, paused execution, restart and retry, duplicate/replayed requests after completion, and races with advance. Expose stop_requested in views.

- [x] 4.3 Extend retry attention gating to permit queuing eligible retries under condition-action/exhaustion holds without releasing dispatch. Verify stop-then-retry and retry-then-stop across sibling loops, two simultaneous condition holds resolved by separate stops, retry under an exhausted sibling, and extension/override preserving unrelated reasons and pause. Preserve cleanup, descendant-consumption, artifact/storage, and judge safeguards. Add config acceptance/rejection tests for transitive aggregate/singleton condition tasks, uncovered body branches, and on_exhaustion without a condition.

## 5. Views

- [x] 5.1 Expose condition state: loop views carry `until_task`, `effective_max_iterations`, and `last_condition_verdict`; attention reasons include the intervention hints. Verify API view tests for a conditioned loop mid-run, held on exhaustion, and after extension; loopless and static-loop views unchanged.

## 6. Documentation and examples

- [x] 6.1 Document conditions in README.md: `until_task`/`on_verdict`/`on_exhaustion` with the break/continue/needs_attention vocabulary and exhaustion choices needs_attention (default) or succeed, the unique aggregate condition-task rule, rejection of on_exhaustion without a condition, needs_attention priority at the cap, independent resolution of concurrent holds, and the mandatory condition-task rule (`allowed_to_fail` omitted/false) and unchanged optional-reviewer tolerance, explicit information-request verdict declarations and until_task mappings, with no special blocked semantics, the extend and stop controls (stop finishes the current iteration, preserves intervention requirements, and never starts another), the effective cap and the no-auto-infinite-loops guarantee, the lack of downgrade support, and the deferred "answer and continue" note (no automatic answer injection or successful-attempt rerun).
- [x] 6.2 Extend an example (or add `examples/workflow-loop-conditions.yaml`) with a review loop that continues on `issues_found` and breaks on `review_passed`, and confirm all examples still load.

## 7. Final checks

- [x] 7.1 Run `gofmt` on changed Go files, `go vet ./...`, and `go test -race -count=1 ./...`; all green.
- [x] 7.2 Run `openspec validate add-workflow-loop-conditions --strict` and walk every delta scenario against the implementation and tests before archiving.
